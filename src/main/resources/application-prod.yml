spring:
  application:
    name: EmployeeManagement
  datasource:
    url: jdbc:mysql://localhost:3306/ems_prod
    username: ems_prod_user
    password: ems
    platform: mysql
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: false
